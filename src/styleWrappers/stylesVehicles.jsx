import styled from "styled-components";

const Wrapper = styled.main`
  /* table {
    margin-bottom: 3rem;
  }
  
  .btn-vehicle-submit {
  align-self: center;
    width: 100%;
    height: 4rem;
  }
  */

  .openModal {
    visibility: hidden;
  }
`;

export default Wrapper;
